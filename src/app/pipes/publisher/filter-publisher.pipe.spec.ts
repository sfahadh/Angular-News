import { FilterPublisherPipe } from './filter-publisher.pipe';

describe('FilterPublisherPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPublisherPipe();
    expect(pipe).toBeTruthy();
  });
});
